---
import type { CollectionEntry } from "astro:content";
import { render } from "astro:content";
import "../styles/global.css";

interface props {
  project: CollectionEntry<"project">;
}

const { project } = Astro.props;
const title = project.data.title;
const dateRange = project.data.dateRange;
const technologies = project.data.technologies;
const { Content } = await render(project);
---

<a href="https://github.com/JLZ22/Rugby-Infinite-Passing.git" target="_blank">
  <div
    class="mt-4 rounded-md border border-slate-400 p-4 transition-all ease-in-out hover:scale-105 hover:bg-slate-500"
  >
    <div class="flex flex-row justify-between">
      <div>
        <span class="text-2xl">{title}</span> - <span class="text-2xl italic"
          >{technologies}</span
        >
      </div>
      <span class="text-lg italic">
        {dateRange}
      </span>
    </div>

    <br />

    <div>
      <Content />
    </div>
  </div>
</a>
